import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic9.starter',
  appName: 'Juego de movimiento kinético',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
