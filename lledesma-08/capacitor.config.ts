import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic8.starter',
  appName: 'Visualizador kinético',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
