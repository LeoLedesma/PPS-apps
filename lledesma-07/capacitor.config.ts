import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic7.starter',
  appName: 'Administración de usuarios',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
