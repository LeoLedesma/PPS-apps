import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic10.starter',
  appName: 'Control de gastos',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
